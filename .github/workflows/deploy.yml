name: Build and Deploy

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Build Frontend
        working-directory: ./frontend
        run: npm run build
        env:
          VITE_API_URL: ${{ secrets.PRODUCTION_API_URL }}

      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: frontend-dist
          path: ./frontend/dist

  build-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        working-directory: ./backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # Add testing steps here once tests are implemented

  # Deploy step example (Uncomment and configure for specific AWS service like ECS or Amplify)
  # deploy:
  #   needs: [build-frontend, build-backend]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Deploy to AWS
  #       run: echo "Deployment script goes here"
